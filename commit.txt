Commit

Steps included in commit
-> Working Directory 
    (git add)
-> Staging Area
    (git commit)
-> git Repository

git add fileName
git add file1Name file2Name
git add . 
(to add all the files)

git commit -m "commit message" 

git commit 

Set VS Code as Default
->git config --global core.editor "code --wait" 
-> Install Shell Command

Atomic Commit
-> It Suggests that each commit should represent only one feature or one fix

Writing Commit message
-> Present Tense & Imperative Stlye

Amending commits
-> git commit --amend
-> useful when we want to undo a commit and modify the commit

git commit -a -m "add and commit in the same time"
-> used to add and commit at the same time

